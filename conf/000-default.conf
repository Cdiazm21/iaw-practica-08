<VirtualHost *:80>
    # Dejamos la configuración del VirtualHost como estaba
    # sólo hay que añadir las siguiente directivas: Proxy y ProxyPass

    <Proxy balancer://mycluster>
        # Server 1
        BalancerMember http://IP_HTTP_SERVER_1(18.205.119.30)

        # Server 2
        BalancerMember http://IP_HTTP_SERVER_2(3.95.187.232)
    </Proxy>

    ProxyPass / balancer://mycluster/
</VirtualHost>